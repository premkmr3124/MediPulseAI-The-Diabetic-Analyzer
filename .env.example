# MongoDB Atlas connection string
# Replace <username>, <password>, and <cluster-url> with your Atlas credentials
MONGO_URI=mongodb+srv://<username>:<password>@<cluster-url>/medipulse?retryWrites=true&w=majority

# Flask secret key (change this to a long random string in production)
SECRET_KEY=medipulse-secret-change-in-prod
